/**
 * Jest Global Setup
 *
 * Runs once before the entire test suite.
 * Sets common environment variables for a clean, isolated test environment.
 */
process.env.NODE_ENV = 'test';
process.env.LOG_LEVEL = 'silent';
